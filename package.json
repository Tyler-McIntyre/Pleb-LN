{
  "dependencies": {
    "@radar/lnrpc": "^0.11.1-beta.1",
    "firebase": "^9.6.11"
  },
  "scripts": {
    "build": "npm install && flutter pub get",
    "test": "flutter test",
    "rebuild": "npm ci && flutter clean && npm run build_flutter",
    "build_flutter":"flutter pub get"
  }
}
